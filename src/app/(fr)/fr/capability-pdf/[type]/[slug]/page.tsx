import {
  default as Page,
  generateStaticParams,
  createCapabilityPdfMetadata,
} from "../../../../../(en)/capability-pdf/[type]/[slug]/page";

export { generateStaticParams };
export default Page;
export const generateMetadata = createCapabilityPdfMetadata("fr");
